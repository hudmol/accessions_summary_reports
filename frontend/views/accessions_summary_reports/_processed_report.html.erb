<h2><%= I18n.t("plugins.accessions_processed_report.title") %></h2>

<div>
  <table class="table table-striped table-bordered table-condensed">
    <tbody>
    <tr> 
      <th><%= @params['start_date'] %> &mdash; <%= @params['end_date'] %></th>
      <th><%= I18n.t("plugins.accessions_processed_report.number") %></th>
      <th><%= I18n.t("plugins.accessions_processed_report.extent") %></th>
    </tr>
    <tr>
      <th><%= I18n.t("plugins.accessions_processed_report.total") %></th>
      <td><%= @data['total_accs'] %></td>
      <td><%= @data['total_extent'].round(2) %></td>
    </tr>
    </tbody>
  </table>
</div>

<div>
  <table class="table table-striped table-bordered table-condensed">
    <tbody>
    <tr> 
      <th><%= I18n.t("plugins.accessions_processed_report.unprocessed") %></th>
      <th><%= I18n.t("plugins.accessions_processed_report.number") %></th>
      <th><%= I18n.t("plugins.accessions_processed_report.extent") %></th>
    </tr>
    <tr>
      <th><%= I18n.t("plugins.accessions_processed_report.total") %></th>
      <td><%= @data['unprocessed_accs'] %></td>
      <td><%= @data['unprocessed_extent'].round(2) %></td>
    </tr>
    </tbody>
  </table>
</div>

<% unless @data['bad_date'].empty? %>
<br>
<h4><%= I18n.t("plugins.accessions_processed_report.bad_date_header") %></h4>
<div>
  <ul>
  <% @data['bad_date'].each do |id| %>
    <% acc = JSONModel(:accession).find(id)["display_string"] %>
      <li><%=  link_to acc, {:controller => :accessions, :action => :show, :id => id} %></li>
  <% end %>
  </ul>
</div>
<% end %>
