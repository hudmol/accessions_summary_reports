
<%= form_for @accessions_summary_report, :as => "accessions_summary_report", :url => {:action => :create}, :html => {:class => 'form-horizontal aspace-record-form'} do |f| %>

  <%= form_context :accessions_summary_report, @accessions_summary_report do |form| %>

<div class="row-fluid">
   <div class="span3">
   </div>
   <div class="span9">
     <div class="record-pane">
       <h2><%= I18n.t("plugins.accessions_summary_reports.title") %></h2>
    </div>
     <div class="row-fluid">
       <div class="span12">
         <%= render_aspace_partial :partial => "accessions_summary_reports/form", :locals => {:form => form} %>
       </div>
     </div>
  </div>
</div>

  <% end %>
<% end %>
